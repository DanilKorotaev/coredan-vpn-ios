# App identifier and team — override via env for CI.
# See docs/FASTLANE.md

app_identifier(ENV.fetch("APP_IDENTIFIER", "com.coredan.CoreDanVPN"))

# Optional until you use lanes that talk to Apple ID directly (e.g. some match flows):
# apple_id("you@example.com")

# Apple Developer Team ID — gym export needs this when Xcode omits team from archive metadata:
team_id(ENV.fetch("TEAM_ID", "66C9VGAZR5"))
