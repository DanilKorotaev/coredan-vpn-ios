// Copy to Secrets.xcconfig (gitignored). Optional local overrides for Debug/Release.
// Team ID is set in project.yml (same Apple account as Knowledge Base App).
// CI may generate Secrets.xcconfig with DEVELOPMENT_TEAM from the TEAM_ID secret.
